# Legacy Compatibility Memo Area

`experiments/memos/` is retained only for compatibility with older memo-generation helpers.

## Status

- Compatibility-only.
- Not the canonical v2 history location.

## Canonical Path

Prefer:

- `history/experiments/` for detailed experiment history
- `EXPERIMENT_LOG.md` for concise indexing
- `MEMORY.md` for current state updates derived from memo analysis
