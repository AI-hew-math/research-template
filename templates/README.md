# Deprecated Compatibility Asset

This legacy top-level README template is no longer used for new projects.

Use:

- `templates/base/README.md`
- `templates/shared/reviews/README.md`
- `templates/shared/skills/`

Then apply an optional profile overlay from `templates/profiles/`.
