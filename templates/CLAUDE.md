# Deprecated Compatibility Asset

This legacy top-level template file is no longer the source of truth for new projects.

Use these v2 paths instead:

- `templates/base/AGENTS.md`
- `templates/base/CLAUDE.md`

New projects scaffold `AGENTS.md` as the canonical instruction file and keep `CLAUDE.md` as a thin wrapper.
