# Deprecated Compatibility Asset

This legacy top-level experiment log template is no longer used for new projects.

Use:

- `templates/profiles/research/EXPERIMENT_LOG.md`

In v2, `EXPERIMENT_LOG.md` is ledger-only and must not become a history dump.
